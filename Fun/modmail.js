module.exports = {
    name: 'modmail',
    permissions: [],
    description: " Say Command!",
    async execute(client, message, cmd , args, Discord) {
        let msgargs = args.join(' ')
        
    }
}
